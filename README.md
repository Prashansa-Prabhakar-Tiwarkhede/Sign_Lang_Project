This project implements a real-time American Sign Language (ASL) alphabet 
recognition system using hand landmark coordinates extracted from MediaPipe.

Instead of using raw images and CNNs, this system uses:

21 hand landmarks
(x, y, z) coordinates
63 structured numerical features
Deep Neural Network (MLP)
Feature standardization
